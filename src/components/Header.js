import React from "react";

const Header = () => (
    <div class="container">
        <header className="jumbotron">
            <h1>Team Selector</h1>
        </header>
    </div>
);

export default Header;